/*
Lab 8: State Machine Validator

States: OPEN, IN_PROGRESS, PENDING_APPROVAL, CLOSED
Roles: ANALYST, SUPERVISOR
*/

const VALID_TRANSITIONS = {
  // TODO: Define transitions and required roles
};

function isValidTransition(currentState, newState, role) {
  // TODO: Return boolean
  return false;
}

module.exports = { isValidTransition };
