# Flashcards — <Lab Title>

> Review **day 1 → 3 → 7 → 14** (roughly doubling). Reset a card to day 1 on a miss.
> 8–15 cards. Each cue must require **free recall** (no visible answer, no multiple choice). Cards cover **reusable primitives**, not lab trivia.

**Q:** <generative cue, e.g. "When should you reach for an idempotency key, and what makes it work?">
**A:** <concise model answer>

**Q:** …
**A:** …

<!-- repeat to 8–15 cards. Good primitive themes:
 - a decomposition heuristic (ask + assume; entities = nouns; state ≠ UI)
 - a company value/LP and what behavior demonstrates it
 - an API/data primitive (idempotency, pagination, cursor vs offset, webhook verification)
 - a complexity fact (top-K = heap O(n log k))
 - a metric primitive (NSM vs guardrail; precision vs recall)
 - a failure-mode + mitigation (stale data → timestamps + dead-man's switch)
-->
